<table class="table">
  <thead>
    <tr>
      <th>Org</th>
    </tr>
  </thead>

  <tbody>
    <% @orgs.each do |org| %>
      <tr>
        <td><%= link_to org.login, repos_path(org: org.login) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
